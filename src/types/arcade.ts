export interface Arcade {
  name: string;
  operation_time: string;
  holiday: string;
  latitude: string;
  longitude: string;
  tel: string;
  address: string;
  access: string;
}

export interface ArcadesData {
  shops: Arcade[];
}

